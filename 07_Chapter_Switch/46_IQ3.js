//For below example both the cases are same i.e. case 10 so it will always
//execute the 1st matched case and will give that output

let x=10;

switch (x)
{
    case 10:
        let b1=1;
        console.log(b1);
        break;
    case 10:
        let b2=2;
        console.log(b2);
        break; 
    default:
        console.log("d");

}//OP - 1