// console.log(MAX_RETRIES); // TDZ
// const MAX_RETRIES = 3;
// // MAX_RETRIES = 4;

const score = 100
// score = score+10;// Assignment to constant variable are not allowed

const pi = 3.14;
const prod_api_url = "https://app.vwo.com/#login";// we can assign the variable value like this but we cannot reassign the value of prod_api_url
const qa_api_url = "https://qa.vwo.com/#login";// we can assign the variable value like this but we cannot reassign the value of qa_api_url

let abc = "anil";
console.log(abc); // anil